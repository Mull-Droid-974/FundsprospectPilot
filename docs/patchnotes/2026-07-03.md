## Patchnotes — 03.07.2026

### Fondsuniversum über Morningstar geprüft & bereinigt

**Universums-Check via Morningstar-MCP**
Das gesamte Fondsuniversum (24.726 ISINs) wurde über die Morningstar-Direct-MCP-Schnittstelle geprüft: **aktive Klasse** (`OS999` Status = „Active") **und Vertriebsland Schweiz** (`OS018` „Country Registered for Sale" enthält „Switzerland"). Pro ISIN: id-lookup → Fonds-ID → data-tool.

**Ergebnis & Bereinigung**
- ✅ **23.401 behalten** (94,6 %) — aktiv **und** CH-Vertrieb
- ❌ **1.325 entfernt** (5,4 %): 694 inaktiv & kein CH · 597 kein Morningstar-Treffer · 25 kein CH-Vertrieb · 9 inaktiv
- Die DB wurde entsprechend bereinigt (Backup unter `data/output/results.db.bak_*` angelegt).
- Dokumentation: `FONDSUNIVERSUM_MS_PRUEFUNG.md` + `data/output/fondsuniversum_ms_pruefung.csv`.

### Neue Fondsstruktur-Ansicht (angereichert & geprüft)

**Hierarchie Umbrella → Portfolio → Anteilsklasse**
Neues Fenster **„🗂 Struktur-Ansicht"** (Button im Ergebnis-Fenster): aufklappbarer Baum
**Umbrella (Morningstar-Branding) → Portfolio (Subfonds) → Anteilsklasse**. Datenschicht `struktur_store.py`, GUI `struktur_window.py`.

**Morningstar-Anreicherung**
Der MS-Store wurde um die fehlenden Identifier/Namen ergänzt (nachgeladen via MCP):
**SecId**, **Name** (`OS01W`) und **Umbrella/Branding** (`FC001`) — zusätzlich zu den vorhandenen Feldern Kategorie, Status, Domizil, Rating. Angezeigt werden je Klasse: ISIN · SecId · Kategorie · Status · Domizil · Währung · TER · Rating.

**Konsistenz-Prüfung (🟢/🟡/🔴)**
Jede Klasse wird geprüft und der schlechteste Status nach oben (Subfonds/Umbrella) durchgereicht:
fehlende Identifier, **Namensabweichung Prospekt ↔ Morningstar**, leere Anteilsklassen-Bezeichnung, fehlende MS-Daten. Auffällige Umbrellas stehen oben. Export der Befunde: `STRUKTUR_PRUEFUNG.md` + `data/output/struktur_pruefung.csv`.
Stand: 21.885 🟢 · 1.515 🟡 · 1 🔴.

**Umbrella einheitlich in beiden Menüs**
Die Umbrella-Spalte im Prospekte-Menü kommt jetzt ebenfalls aus dem Morningstar-Branding
(`morningstar_store.get_umbrella_map()`) statt aus einer Fondsname-Heuristik — Prospekte-Menü und Struktur-Ansicht zeigen damit **dieselbe** Struktur (verifiziert identisch).

### GUIs auf Anthropic-only / nativ aufgeräumt

**Analyse-Fenster**
- Provider-Dropdown (Gemini/OpenRouter) → statisches **„Anthropic · nativ"**
- **„✂ Trim"-Spalte** und `.trimmed.txt`-Logik entfernt
- Button **„Grosse PDFs zurücksetzen"** + `trim_model`-Übergabe + Multi-Provider-API-Key-Logik entfernt (nur noch `ANTHROPIC_API_KEY`)

**Menü & Fenster**
- Menüpunkt **„PDF-Kürzen" entfernt**, `pdf_trim_window.py` gelöscht (basierte komplett auf der alten Text-Extraktion + Gemini/OpenRouter-Kürzung → obsolet, da natives PDF automatisch reduziert). Navigation neu nummeriert: 3. Analyse · 4. Ergebnisse · 5. Vergleich.

**Admin-Panel**
- **Gemini- und OpenRouter-Tabs entfernt** (inkl. Key-Validierung + Save/Load); LLM-Provider fest Anthropic.

> Hinweis: Das Vergleich-Fenster nutzt intern noch die Text-Extraktion und bleibt vorerst unverändert.
